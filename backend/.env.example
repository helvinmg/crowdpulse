# Database (use ONE of these)
# PostgreSQL:
DATABASE_URL=postgresql://postgres:password@localhost:5432/crowdpulse
# MySQL:
# DATABASE_URL=mysql+pymysql://root:password@localhost:3306/crowdpulse

# Redis (optional — only needed for Celery scheduled pipeline)
REDIS_URL=redis://localhost:6379/0

# Telegram
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash
TELEGRAM_SESSION_NAME=crowdpulse

# Twitter / X
TWITTER_BEARER_TOKEN=your_bearer_token

# Reddit (create app at https://www.reddit.com/prefs/apps — choose "script")
REDDIT_CLIENT_ID=your_reddit_client_id
REDDIT_CLIENT_SECRET=your_reddit_client_secret

# LLM Labeling (Google Gemini — free tier)
# Get your key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key

# Auth (JWT)
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRY_HOURS=72

# App
APP_ENV=development
LOG_LEVEL=DEBUG
CORS_ORIGINS=http://localhost:3000
